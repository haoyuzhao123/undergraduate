Homework c Readme
赵浩宇，2016012390

选择做作业中的第一道题目，由paper可知，问题转化为计算两不相交椭球的最小距离
EllipDist为新建函数，用于计算两不相交椭球之间的最小距离，需要输入的椭球为标准的格式，即(x-mu)^T A^(-1) (x-mu) = 1。
对原来的文件进行了适当的修改，把椭球转化为标准格式，并且调用EllipDist求解最小距离，并且在图中进行标注。
